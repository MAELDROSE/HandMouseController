@"
# HandMouseController 🖱️🤚

Control del mouse usando la mano a través de la cámara web, utilizando **MediaPipe**, **OpenCV** y **PyAutoGUI**.

## 🎯 Funcionalidades

- Mover el cursor con la punta del dedo índice.
- **Click izquierdo** con gesto: pulgar doblado, índice extendido.
- **Click derecho** con gesto: índice y meñique extendidos.
- **Click sostenido / drag** al cerrar el puño.
- Registro opcional de coordenadas del cursor en un archivo CSV.

## 🛠️ Requisitos

- Python 3.10 (recomendado)
- Librerías de Python:

```bash
pip install -r requirements.txt
